import { Button } from 'antd';

function ApplyButton() {
  return (
    <Button type="primary" size="small" className="w-full">
      <span className="font-normal text-[0.8rem] leading-[150%]">Apply Filter</span>
    </Button>
  );
}

export default ApplyButton;
